<div id="draw-wrapper">
  <div id="title">{{title}}</div>
  <canvas id="canvas" width="500" height="300"></canvas>
  <div id="instructions">
    Click and hold then move the mouse to draw a line. Release the mouse button
    to finish. Other connected clients will see the line. Lines are removed
    after a short time, to keep the canvas clear.
  </div>
  <div id="status"></div>
</div>
